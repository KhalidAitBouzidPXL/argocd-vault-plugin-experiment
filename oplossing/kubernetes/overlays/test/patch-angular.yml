apiVersion: apps/v1
kind: Deployment
metadata:
  name: angular-client-deployment
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: angular-client-container
        resources:
          limits:
            memory: "2Gi"